# loan_defaulter_prediction
Machine learning pipeline that predicts whether a borrower will default on a loan. Covers EDA, preprocessing, feature engineering, model comparison (baseline → tuned), evaluation (ROC-AUC/F1), explainability, and reproducible experiments.
